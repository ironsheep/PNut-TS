# Map File Verification Tests

## Purpose

Verify that `.map` files generated by the `-m` flag accurately reflect compiled object structure by cross-referencing against:
1. **Listing files (`.lst`)** - Generated by `-l` flag, contains symbol table and hex dump
2. **Expected definitions (`expected.json`)** - Ground truth derived from source file analysis

## Methodology

1. Define test objects with known, predictable structures
2. Capture expectations in `expected.json` for each test
3. Compile with both `-l -m` flags
4. Run `verify-map.ts` to cross-check all three sources (source expectations, listing, map)

## Test Cases

### test1-simple (Basic Parent/Child)
- **Structure**: 2 objects, depth 1
- **Focus**: Basic parent/child relationship, simple method signatures
- **Methods**: `PUB go()`, `PUB add(a, b) : sum`
- **VAR**: LONG and BYTE variables
- **DAT**: Single LONG in child

### test2-deep (Deep Nesting)
- **Structure**: 3 objects, depth 2 (top -> mid -> leaf)
- **Focus**: Deep object hierarchy, multiple return values, local variables
- **Methods**: `PUB run() : x, y`, `PRI helper(p1, p2, p3)`, methods with locals
- **VAR**: Mixed types at each level
- **DAT**: Variables at leaf level

### test3-wide (Breadth with PASM)
- **Structure**: 4 objects, breadth 3 (top with 3 children)
- **Focus**: Sibling objects, PASM labels, DAT varieties
- **Methods**: Mix of all signature forms
- **VAR**: Different types per child
- **DAT**: PASM code with labels in one child

## What We Verify

### Symbol Counts
- Number of DAT symbols in map matches listing
- Number of VAR symbols in map matches listing
- Number of methods in map matches listing

### Address/Offset Consistency
- DAT symbol offsets in map match VALUE field in listing
- VAR symbol offsets in map match VALUE field in listing
- Method entry points in map are past the object's header table

  **Changed in v1.55.4 — this check used to read "match VALUE field in
  listing", and that was the defect, not the check.** The listing's method
  VALUE is the method's **slot index in the object header table**, not an
  address. The map printed that same index in an address column, so the two
  agreed and the check passed *only while the bug was present*. The map now
  prints real hub addresses (`Entry +$rel  ($abs)`), so there is nothing in the
  listing to compare against, and the assertion is now the weaker invariant
  that a method's bytecode begins after the header table
  (`src/tests/MAP-tests/verify-map.ts`). Strengthening it to the exact
  identity `abs == object code base + rel` — both of which the map itself
  prints — is tracked in `DOCs/roadmaps/Test-Suite-Punch-List.md`.

### Size Totals
- `OBJ bytes` in listing matches executable size in map
- `VAR bytes` in listing matches variable size in map

### Object Hierarchy
- Object count in map matches expected
- Object names correlate correctly with file IDs

### PASM Labels
- PASM labels appear in both DAT Sections and PASM Labels sections
- COG addresses calculated correctly (DAT offset / 4)

### Method Signatures (Future Enhancement)
- Parameter counts match source
- Return value counts match source
- Local variable counts match source

## expected.json Schema

```json
{
  "description": "Brief description of this test case",
  "objects": [
    {
      "name": "object_name",
      "file": "object_name.spin2",
      "methods": [
        {
          "name": "methodName",
          "visibility": "PUB|PRI",
          "params": 0,
          "returns": 0,
          "locals": 0
        }
      ],
      "vars": [
        {"name": "varName", "type": "LONG|WORD|BYTE", "count": 1}
      ],
      "dat": [
        {"name": "datName", "type": "LONG|WORD|BYTE", "count": 1}
      ],
      "pasm_labels": [
        {"name": "labelName"}
      ]
    }
  ],
  "totals": {
    "object_count": 2,
    "method_count": 3,
    "var_symbols": 4,
    "dat_symbols": 2,
    "pasm_labels": 0
  }
}
```

## Running Tests

```bash
# Run map verification tests
npm run test-map

# Or run directly
npx jest --config jest-config/jest.map.config.js
```

## Files

- `README.md` - This test plan
- `verify-map.ts` - Verification script that parses and cross-checks files
- `test1-simple/` - Simple parent/child test
- `test2-deep/` - Deep nesting test
- `test3-wide/` - Wide hierarchy with PASM test

## Verification Script Output

```
=== Map Verification: test1-simple ===

Parsing expected.json... OK (2 objects, 3 methods, 2 vars, 1 dat)
Parsing simple_top.lst... OK
Parsing simple_top.map... OK

Cross-checking symbols:
  [PASS] DAT symbol count: expected 1, lst 1, map 1
  [PASS] VAR symbol count: expected 2, lst 2, map 2
  [PASS] Method count: expected 3, lst 3, map 3
  [PASS] OBJ bytes: lst 48, map 48
  [PASS] VAR bytes: lst 8, map 8
  [PASS] Symbol 'counter' offset: lst $00004, map $00004

All checks passed!
```
